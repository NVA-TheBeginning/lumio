{
  "name": "plagiarism-service",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "cargo build",
    "start": "cargo run",
    "dev": "cargo watch -x run",
    "start:prod": "cargo run --release",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets --all-features --fix --allow-dirty --allow-staged -- -D warnings"
  }
}
